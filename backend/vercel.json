{
  "version": 2,
  "functions": {
    "api/**/*.js": {
      "memory": 256,
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/health", "dest": "/api/health" },
    { "src": "/snaptrade/(.*)", "dest": "/api/snaptrade/$1" }
  ]
}